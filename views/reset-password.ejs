<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Reset Password - PlayBell</title>
  <style>
    body{margin:0;padding:0;background:radial-gradient(circle at top,#1a1a1a,#050505);
      font-family:Arial,sans-serif;height:100vh;display:flex;align-items:center;justify-content:center;color:#fff;}
    .auth-wrapper{width:100%;display:flex;justify-content:center;align-items:center;padding:10px;box-sizing:border-box;}
    .auth-card{width:100%;max-width:380px;background:rgba(10,10,10,0.95);border-radius:14px;padding:24px;
      box-shadow:0 0 25px rgba(0,0,0,0.8);border:1px solid #222;}
    .auth-card h2{text-align:center;margin-bottom:6px;color:#00ffcc;}
    .auth-helper{text-align:center;font-size:13px;color:#aaa;margin-bottom:16px;}
    .auth-form label{display:block;margin-bottom:12px;font-size:13px;}
    .auth-form input{width:100%;padding:9px;margin-top:4px;border-radius:6px;border:none;outline:none;
      font-size:14px;background:#1b1b1b;color:#fff;}
    .auth-form input:focus{box-shadow:0 0 0 1px #00ffcc;}
    .auth-submit{width:100%;padding:10px;margin-top:8px;background:#00ffcc;color:#000;border:none;border-radius:6px;
      font-weight:bold;cursor:pointer;}
    .auth-submit:hover{background:#00e6b8;}
    .error{background:#ff4444;color:#fff;padding:8px;border-radius:5px;text-align:center;margin-bottom:10px;font-size:13px;}
    .success{background:#2e7d32;color:#fff;padding:8px;border-radius:5px;text-align:center;margin-bottom:10px;font-size:13px;}
    .auth-links{text-align:center;margin-top:10px;font-size:13px;}
    .auth-links a{color:#00ffcc;text-decoration:none;}
    .auth-links a:hover{text-decoration:underline;}
  </style>
</head>
<body>

<div class="auth-wrapper">
  <div class="auth-card">
    <h2>Reset Password</h2>

    <% if (error) { %><div class="error"><%= error %></div><% } %>
    <% if (success) { %><div class="success"><%= success %></div><% } %>

    <% if (!success && token) { %>
      <form class="auth-form" method="POST" action="/reset-password">
        <input type="hidden" name="token" value="<%= token %>">

        <label>
          New Password
          <input type="password" name="newPassword" required>
        </label>

        <label>
          Confirm Password
          <input type="password" name="confirmPassword" required>
        </label>

        <button type="submit" class="auth-submit">Update Password</button>
      </form>
    <% } %>

    <div class="auth-links">
      <a href="/login">Back to Login</a>
    </div>
  </div>
</div>

</body>
</html>
