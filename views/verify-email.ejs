<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Verify Email - PlayBell</title>
  <style>
    body{margin:0;padding:0;background:radial-gradient(circle at top,#1a1a1a,#050505);
      font-family:Arial,sans-serif;height:100vh;display:flex;align-items:center;justify-content:center;color:#fff;}
    .auth-wrapper{width:100%;display:flex;justify-content:center;align-items:center;padding:10px;box-sizing:border-box;}
    .auth-card{width:100%;max-width:380px;background:rgba(10,10,10,0.95);border-radius:14px;padding:24px;
      box-shadow:0 0 25px rgba(0,0,0,0.8);border:1px solid #222;text-align:center;}
    .auth-card h2{margin-bottom:8px;color:#00ffcc;}
    .msg{font-size:14px;margin-bottom:16px;color:#ddd;}
    .success{color:#00ffcc;font-weight:bold;margin-bottom:10px;}
    .error{color:#ff6666;font-weight:bold;margin-bottom:10px;}
    a{color:#00ffcc;text-decoration:none;}
    a:hover{text-decoration:underline;}
  </style>
</head>
<body>

<div class="auth-wrapper">
  <div class="auth-card">
    <h2>Email Verification</h2>

    <% if (status === "success") { %>
      <div class="success">✅ Your email has been verified.</div>
      <p class="msg">Now wait for admin to verify your account role. Then you can login.</p>
    <% } else { %>
      <div class="error">❌ Invalid or expired verification link.</div>
      <p class="msg">Try registering again or contact admin.</p>
    <% } %>

    <p><a href="/login">Go to Login</a></p>
  </div>
</div>

</body>
</html>
